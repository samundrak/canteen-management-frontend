<div class="form-group">
  <label for="email">{{ label }}:</label>
  {{yield}}
  {{#if errors}}
    {{#each errors as |error|}}
      <div class="message message-danger" style="color:red">
        {{ error }}
      </div>
    {{/each}}
  {{/if}}
</div>